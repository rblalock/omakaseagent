# Omakase scenario evals

Mechanical contracts that pin skill and native-agent prompts to expected behavior. Each `*.eval.json` names target files and regex patterns that must hold after `npm run build`.

Run: `npm run verify:scenario-evals`
